# Session Journal — 2026-03-16

## Goal

Process KADE agent memos, design and implement knowledge durability (dual-write), rewrite README.

## What Was Done

### 1. KADE Memo Triage

Received 7 memos from KADE agent (written to context-hooks DB). 37 garbled overflow rows caused by multi-line CLI parsing bug — reconstructed 3 memos, deleted garbage, sent status reply to KADE2.

Triage: 4 already resolved (MCP tests, cross-project routing, shared_state tests, priority column fix), 3 new items added to todo (v1 DB path migration, session-start health check adapter, short_hash silent failures), 1 design input captured (knowledge as pointers with file_refs).

### 2. Multi-line Memo Fix

Added `--content -` stdin support to `parse_memo_send_args()`. 4 new tests. The root cause of garbled memos: KADE agent piped multi-line content that got split into separate positional args, each becoming its own memo row.

### 3. Knowledge Durability Design

Brainstormed dual-write approach through design spec. Key decisions:
- Export to master project's repo (`data/knowledge/` by default)
- Opt-in via `knowledge_export: true` in config
- Markdown files with YAML frontmatter (concise pointers, not content dumps)
- SQLite is authoritative, markdown is read-only export
- Same-title supersede uses file rename to preserve both entries

Spec at `docs/superpowers/specs/2026-03-16-knowledge-durability-design.md`. Two review passes caught 12 issues (3 blocking: resolve_export_dir body, frontmatter fence parsing, dismiss flow missing fetch; 3 important: supersede field inheritance, archive/dismiss asymmetry, same-title collision).

### 4. Knowledge Durability Implementation

Plan at `docs/superpowers/plans/2026-03-16-knowledge-durability.md`. Plan review caught 6 more issues (2 blocking: write_entry needed filename_override for supersede, missing imports in CLI; 2 important: MCP handlers and CLI main() not passing export_dir).

Implementation: 3 chunks, 12 tasks.

- `lib/export.py` — New module: _slugify, _render_frontmatter, _fetch_entry, resolve_export_dir, write_entry (with filename override), remove_entry, write_index, export_all, _safe_export
- `lib/knowledge.py` — All 6 mutations accept `export_dir`, CLI main() resolves from config, new `export` command
- `lib/mcp_tools.py` — All mutation handlers pass export_dir, supersede extended with ref fields
- 26 unit tests (test_export.py) + 11 integration tests (test_knowledge.py)

### 5. README Rewrite

Complete rewrite: architecture diagram, 7 detailed workflows (bug fix flywheel, cross-project sharing, knowledge durability, memo exchange, parallel path detection, compaction survival, MCP server), full command reference, configuration guide.

### 6. Results

- 279 tests passing (up from 243). 36 new tests.
- 3 commits.
- ~1600 lines added.

## What Needs Dogfooding

The knowledge durability system is implemented but hasn't been tested end-to-end in a real project. Key dogfooding steps:

1. Enable `knowledge_export: true` on KADE2 (the master project with 14 knowledge entries)
2. Run `context-hooks knowledge export` and verify all 14 entries export correctly
3. Store a new knowledge entry and verify both SQLite and markdown update
4. Promote/archive/supersede and verify the markdown files update correctly
5. Test the MCP path: use agent-bridge compat to store knowledge and verify export fires
6. Test cluster path: from context-hooks satellite, store knowledge and verify it exports to KADE2's repo

## Session End

**Status:** Knowledge durability implemented, README rewritten, all tests passing. Next: dogfood the export on KADE2's real knowledge entries, then pick up remaining todo items (E2E MCP test, v1 migration, session-start health check adapter).
